"use strict";

const searchValue = document.getElementById('search-input');

class Search {
    constructor() {
    }

    onType() {
        search.addEventListener('keyup', function (ev) {
        });
    }
}

searchValue.addEventListener('keyup', function (ev) {
    ev.preventDefault();
    let exist = new Search();
}, true);